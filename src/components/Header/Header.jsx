// @flow
import PropTypes from 'prop-types';
import React from 'react';
import styled from 'styled-components';

const HeaderContainer = styled.header`
  color: white;
  font-size: 2vw;
  font-weight: bold;
`;

function Header() {
  return <HeaderContainer>bc-010</HeaderContainer>;
}

export default Header;
